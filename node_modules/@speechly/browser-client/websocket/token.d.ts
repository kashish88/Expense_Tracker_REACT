declare type fetchFn = (input: RequestInfo, init?: RequestInit) => Promise<Response>;
declare type nowFn = () => number;
export declare const minTokenValidTime: number;
export interface Token {
    appId: string;
    deviceId: string;
    configId: string;
    scopes: string[];
    issuer: string;
    audience: string;
    expiresAtMs: number;
}
export declare function fetchToken(baseUrl: string, appId: string, deviceId: string, fetcher?: fetchFn, nowFn?: nowFn): Promise<string>;
export declare function validateToken(token: string, appId: string, deviceId: string, now?: nowFn): boolean;
export declare function decodeToken(token: string): Token;
export {};
